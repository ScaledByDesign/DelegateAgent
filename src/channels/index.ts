// Channel self-registration barrel file.
// Each import triggers the channel module's registerChannel() call.

// delegate (Delegate workspace platform — primary channel)
import './delegate.js';

// discord

// gmail

// slack

// telegram

// whatsapp
